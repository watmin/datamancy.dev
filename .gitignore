node_modules/
.DS_Store
# Defensive: never accept .pem or .key into this repo.
# Private keys live offline at ~/.config/datamancy/.
*.pem
*.key
# Patch / editor cruft — never let `git add .` sweep these into a signed publish.
*.rej
*.orig
*.swp
*.swo
*~
*.bak
