# Cloudflare Pages redirects.
#
# The root now serves a thin HTML landing (index.html) instead of redirecting
# to /grimoire/SKILL.md. WebMCP requires a real HTML page that runs JavaScript
# (it calls navigator.modelContext.provideContext via /webmcp.js) — a 302 to a
# text/markdown file gives the agent-readiness crawler nothing to execute and it
# times out. The landing links straight to the grimoire; spells stay raw markdown.
#
# (No redirects currently needed.)
